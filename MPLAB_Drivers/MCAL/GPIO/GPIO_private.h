#ifndef GPIO_PRIVATE_H
#define GPIO_PRIVATE_H

/*
 * GPIO_private.h - MPLAB XC8 Compatible
 *
 * In XC8, all PIC16F877A Special Function Registers (TRISA, PORTA, etc.)
 * are declared automatically when you include <xc.h>. The manual address-
 * cast definitions used in mikroC are removed to avoid redefinition conflicts.
 */

/* All register symbols (TRISA, TRISB, ..., PORTA, PORTB, ...) are
   provided by <xc.h> which is included via System.h or GPIO_interface.h. */

#endif
